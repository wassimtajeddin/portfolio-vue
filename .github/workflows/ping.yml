name: Keep Render Alive

on:
  schedule:
    - cron: "*/11 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://portfolio-vue-1hg5.onrender.com/
